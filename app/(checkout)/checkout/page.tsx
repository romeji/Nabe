import FormulaireCheckout from './FormulaireCheckout';

export const metadata = {
  title: 'Finaliser ma commande — Nabe',
};

export default function PageCheckout() {
  return <FormulaireCheckout />;
}
